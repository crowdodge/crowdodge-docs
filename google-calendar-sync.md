# Google カレンダー同期方式

> システム: crowdodge（チーム「なせばなる」）
> 対象: Google カレンダーとの予定同期・連携の仕様

## 1. 基本方針

- カレンダーの Source of Truth は Google カレンダー。繰り返しルール・例外展開は Google が所有し、本システムは保持しない。
- 本システムは個別の予定（`singleEvents=true` で展開された単発インスタンス）のみを扱う。
- サーバは混雑予測・目的地推定・リマインドのために、近未来のローリング窓に予定を投影として保持する。
- モバイルアプリは Google カレンダーを直接読み取り、サーバが算出した付加情報（混雑・目的地・通知状態）を重ねて表示する。

## 2. 識別子

| 識別子 | 発行元 | 用途 |
|---|---|---|
| google_event_id | Google | 予定インスタンスの識別。モバイル・サーバ・Google 間の突合キー |
| recurring_event_id | Google | 繰り返しシリーズ（マスタ）の識別。単発予定では持たない |
| original_start | Google | 発生回の元の開始時刻（RECURRENCE-ID）。編集スコープの分割点 |
| etag | Google | 予定のバージョン。競合検出・無限ループ防止に使用 |

## 3. 読み取り経路

### 3.1 モバイル

- ユーザーの Google サインインを用い、Google カレンダー API を直接呼び出して予定を取得する（`singleEvents=true`）。
- 全期間・全フィールドを取得でき、表示はサーバの保持期間に依存しない。
- 端末ローカルにキャッシュを保持し、オフライン時はキャッシュを表示する。
- 混雑・目的地・通知状態は、表示中の予定の `google_event_id` をキーにサーバへ問い合わせて重ねる。サーバの窓外（未エンリッチ）の予定は付加情報なしで表示する。

### 3.2 サーバ

- バックグラウンド処理（混雑予測・目的地推定・リマインド）のため、Google カレンダー API を独自に同期し、近未来窓の予定を保持する。
- オフライン継続のため、サーバはリフレッシュトークンを保持する。

## 4. 更新検知

### 4.1 モバイル

- 端末ローカルに保持した同期トークンで増分同期する。
- 同期タイミング: アプリ前面化時 / Pull-to-refresh / バックグラウンド定期取得。

### 4.2 準リアルタイム通知

- サーバは各カレンダーに対し Google Push の watch チャンネルを登録する。
- Google からの変更通知（webhook）をサーバが受信すると、増分同期を行い、対象ユーザーの端末へ FCM で「同期ヒント」データメッセージを送る。
- モバイルはこれを契機に Google へ増分同期する。
- Google Push は端末へ直接届かないため、サーバの watch と FCM が中継の役割を担う。
- watch チャンネルは失効するため、サーバは失効前に再登録する。

## 5. サーバ側ローリング窓の同期

Google カレンダー API の `events.list` は、増分同期トークンと期間指定（`timeMin`/`timeMax`）を併用できない。サーバは次の2系統で同期する。

1. **増分同期**: 同期トークンのみで呼び出し、既知範囲の追加・変更・削除・外部編集を取り込む。
2. **窓送り全同期**: 定期的に `[現在, 現在+N]` の期間を `singleEvents=true` で取得し、新たに窓へ入った将来の予定を投影に追加する。あわせて同期トークンを再取得し、実体化済みの将来端を更新する。

- ローリング窓のサイズ N は設定値とする。
- Google 側で予定が削除/中止された場合、対応する予定を投影から除去し、関連する通知は `cancelled` とする。

## 6. 書き込み（双方向）

アプリ内編集は Google へのコマンドとして反映し、再同期で投影を更新する。書き込みはサーバが仲介する。

| 操作 | 反映方法 |
|---|---|
| 単発予定の編集 | Google API で当該予定を更新 |
| 繰り返し予定「その日のみ」 | Google API で当該インスタンスを更新（Google 側で例外インスタンス化） |
| 繰り返し予定「それ以降すべて」 | Google API でシリーズを分割（マスタの終了日切り詰め＋新シリーズ生成） |

- `recurring_event_id` / `original_start` で対象インスタンス・シリーズを特定する。
- `etag` で「前回取得した版から変化したか」を判定し、競合と無限ループを防止する。

## 7. 目的地推定の共有

目的地は場所に紐づき日付に依存しないため、同一シリーズの全発生回で1つの目的地グループを共有し、推定（ルート計算・LLM 呼び出し）はシリーズで1回のみ実行する。

```
予定の取り込み（destination 推定の契機）
  recurring_event_id が有る:
    同一 recurring_event_id の目的地グループが存在する
      → 既存グループへ予定を紐付け（推定スキップ）
    存在しない
      → 目的地グループを生成（推定実行）し、予定を紐付け
  recurring_event_id が無い（単発）:
    専用の目的地グループを生成（推定実行）し、予定を紐付け
```

- 混雑予測（日付依存）は発生回ごとに行い、共有しない。
- Google 側で特定の発生回のみ場所が変更された場合は、その発生回をシリーズのグループから切り離し、専用グループへ紐付け直す。
