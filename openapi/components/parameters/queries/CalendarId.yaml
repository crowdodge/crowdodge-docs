name: calendarId
in: query
required: false
description: 対象カレンダーID。省略時はユーザーが選択中の全カレンダーを対象とする。
style: form
explode: false
schema:
  type: array
  uniqueItems: true
  items:
    type: string
    minLength: 1
